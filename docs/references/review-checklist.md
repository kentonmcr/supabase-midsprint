# Review Checklist

- [ ] All twelve task requirements working when the app runs with `npm run dev` against a live Supabase project
- [ ] `notes`, `collections`, `tags`, and `note_tags` tables present in Supabase with correct columns and relationships
- [ ] All data operations go through the centralised helper module, not scattered across components
- [ ] At least one optional task completed via a feature branch and pull request
- [ ] One third-party slash command run on a PR diff; finding recorded in REFLECTION.md
- [ ] CLAUDE.md reflects what the agent was actually asked to follow, including Supabase conventions
- [ ] `docs/` contains at least one cited Supabase reference, not a placeholder
- [ ] `.env.local` is not committed; Supabase project URL and anon key are loaded from environment variables
- [ ] Commit history is readable; at least one PR merged with a meaningful diff review and commit
